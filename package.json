{
  "name": "moty",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "wrangler": "wrangler",
    "test:unit": "vitest"
  },
  "dependencies": {
    "firebase": "^9.15.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^1.0.0",
    "@sveltejs/adapter-cloudflare-workers": "^1.0.1",
    "@sveltejs/kit": "^1.0.0",
    "svelte": "^3.54.0",
    "svelte-check": "^2.9.2",
    "tslib": "^2.4.1",
    "typescript": "^4.9.3",
    "vite": "^4.0.0",
    "vitest": "^0.25.3",
    "wrangler": "^2.6.2"
  },
  "engines": {
    "node": "18.x",
    "npm": "8.x"
  },
  "prettier": {
    "arrowParens": "always",
    "printWidth": 160,
    "semi": false,
    "singleQuote": true,
    "tabWidth": 2,
    "trailingComma": "es5"
  }
}
