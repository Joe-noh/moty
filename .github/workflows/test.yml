name: Test

on:
  - push

jobs:
  test:
    runs-on: ubuntu-latest
    name: Unit Test
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'
      - run: npm ci
      - run: npm run test:unit -- --run
